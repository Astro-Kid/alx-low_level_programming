#include <stdio.h>

/**
 * main - base ten in new line format from 0
 * Return: true and output 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
